{
  "name": "sistema-ventas",
  "version": "1.0.0",
  "description": "Sistema de ventas completo",
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "@types/react": "^19.1.10",
    "html2canvas": "^1.4.1",
    "recharts": "^2.15.4",
    "xlsx": "^0.18.5"
  }
}
